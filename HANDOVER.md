# 📋 My Personal Bloomberg - 项目交接文档

## 🎯 项目概述

**项目名称**: My Personal Bloomberg  
**项目类型**: Android 金融资讯终端 + 飞书机器人  
**开发状态**: MVP 阶段完成  
**GitHub**: wang2-lat

---

## 📱 项目组成

本项目包含两个部分：

| 项目 | 功能 | 状态 | 位置 |
|:----|:----|:-----|:----|
| **飞书 Bloomberg V7.0** | 每天早上自动推送新闻分析到飞书群 | ✅ 已部署运行 | GitHub Actions |
| **Android App** | 手机端查看，支持交互和术语解释 | ✅ MVP 完成 | 本地项目 |

---

## ✅ 已完成功能

### 飞书机器人 (V7.0)
- ✅ 每天早上 8:00 EST 自动运行（GitHub Actions）
- ✅ 抓取 WSJ 新闻
- ✅ 获取市场数据（SPY/QQQ/DIA/VIX/费城联储）
- ✅ AI 六维分析（核心判断、因果链、估值视角、风险、建议）
- ✅ 发送富文本卡片到飞书群

### Android App
- ✅ 市场概览卡片（SPY/QQQ/DIA、VIX、费城联储）
- ✅ 新闻分析卡片（4条新闻，含股票数据和 AI 分析）
- ✅ 卡片展开/收起动画
- ✅ 术语解释弹窗（P/E、52周、分析师、VIX、因果链）
- ✅ 深色主题（Bloomberg 风格）
- ✅ 模拟数据可运行

---

## ❌ 未完成功能（需要下一阶段开发）

| 功能 | 优先级 | 说明 |
|:----|:------|:----|
| **Android 实时数据** | 🔴 高 | App 目前使用模拟数据，需连接后端 API |
| **后端 API 部署** | 🔴 高 | 需要把 Python 代码改成 API 格式部署 |
| **UI 科幻美化** | 🟡 中 | 增加科幻感、动画效果、渐变背景 |
| 推送通知 | 🟡 中 | 重大新闻推送到手机 |
| 自选股关注 | 🟢 低 | 用户可关注特定股票 |
| 股票搜索 | 🟢 低 | 搜索任意股票查看分析 |

---

## 📁 项目结构

### Android App 结构
```
my-personal-bloomberg-android/
├── app/src/main/
│   ├── java/com/jeremy/bloomberg/
│   │   ├── MainActivity.kt              # 应用入口
│   │   ├── model/
│   │   │   └── Models.kt                # 数据模型
│   │   ├── data/
│   │   │   └── HomeViewModel.kt         # 状态管理 (MVVM)
│   │   ├── network/
│   │   │   └── ApiService.kt            # API 接口 + 模拟数据
│   │   └── ui/
│   │       ├── theme/Theme.kt           # Bloomberg 主题
│   │       ├── components/
│   │       │   ├── MarketOverviewCard.kt
│   │       │   ├── NewsAnalysisCard.kt
│   │       │   └── TermExplanationDialog.kt
│   │       └── screens/HomeScreen.kt
│   └── res/values/
│       ├── strings.xml
│       ├── colors.xml
│       └── themes.xml
├── build.gradle.kts
└── README.md
```

### 飞书机器人结构
```
bloomberg-feishu-bot/  (在 GitHub 仓库中)
├── push_telegram.py       # 主程序（抓取新闻、分析、发送）
├── .github/workflows/
│   └── main.yml           # GitHub Actions 定时任务
└── README.md
```

---

## 🔧 技术栈

### Android App
| 类型 | 技术 |
|:----|:----|
| 语言 | Kotlin |
| UI 框架 | Jetpack Compose + Material 3 |
| 架构模式 | MVVM |
| 网络库 | Retrofit + OkHttp |
| 异步处理 | Kotlin Coroutines + Flow |
| 最低版本 | Android 8.0 (API 26) |

### 飞书机器人
| 类型 | 技术 |
|:----|:----|
| 语言 | Python 3.10 |
| 数据源 | Finnhub, yfinance, FRED, Alpha Vantage |
| AI | Google Gemini 2.0 Flash |
| 消息 | 飞书开放平台 API |
| 部署 | GitHub Actions (Cron) |

---

## 🔑 API Keys

以下 API Key 已配置在 GitHub Secrets 中：

| Key 名称 | 用途 | 获取地址 |
|:--------|:----|:--------|
| `FINNHUB_KEY` | 股票实时报价、分析师评级 | https://finnhub.io/register |
| `GEMINI_KEY` | AI 分析引擎 | https://aistudio.google.com/apikey |
| `FRED_KEY` | 宏观经济数据（VIX、费城联储） | https://fred.stlouisfed.org/docs/api/api_key.html |
| `ALPHA_VANTAGE_KEY` | 公司基本面数据 | https://www.alphavantage.co/support/#api-key |
| `LARK_APP_ID` | 飞书应用 ID | 飞书开放平台 |
| `LARK_APP_SECRET` | 飞书应用密钥 | 飞书开放平台 |
| `LARK_CHAT_ID` | 飞书群 ID | 飞书群设置 |

---

## 🚀 开发任务指南

### 任务 1：部署后端 API（让 Android App 获取真实数据）

**目标**: 创建一个 API 服务器，让 Android App 可以获取实时数据

**步骤**:

1. 创建 FastAPI 后端项目
```python
# main.py
from fastapi import FastAPI
import finnhub
import yfinance as yf

app = FastAPI()

@app.get("/api/market-overview")
async def get_market_overview():
    # 获取 SPY, QQQ, DIA, VIX 数据
    pass

@app.get("/api/news")
async def get_news():
    # 获取新闻 + AI 分析
    pass

@app.get("/api/stock")
async def get_stock(ticker: str):
    # 获取单只股票数据
    pass
```

2. 部署到 Vercel（免费）
```bash
npm i -g vercel
vercel --prod
```

3. 修改 Android App 的 `ApiService.kt`
```kotlin
// 把这行改成你的 API 地址
private const val BASE_URL = "https://your-api.vercel.app/"
```

---

### 任务 2：UI 科幻美化

**目标**: 让界面更有科技感、更酷炫

**建议改进**:

1. **渐变背景**
```kotlin
// 在 Theme.kt 中
val CyberpunkGradient = Brush.verticalGradient(
    colors = listOf(
        Color(0xFF0D1B2A),  // 深蓝黑
        Color(0xFF1B263B),  // 蓝灰
        Color(0xFF0D1B2A)   // 深蓝黑
    )
)
```

2. **霓虹发光边框**
```kotlin
// 给卡片添加
Card(
    modifier = Modifier
        .border(
            width = 1.dp,
            brush = Brush.linearGradient(
                colors = listOf(Color(0xFF00F5FF), Color(0xFF0080FF))
            ),
            shape = RoundedCornerShape(12.dp)
        )
)
```

3. **数字滚动动画**
```kotlin
// 股价变化时的动画
val animatedPrice by animateFloatAsState(
    targetValue = price,
    animationSpec = tween(durationMillis = 500)
)
```

4. **扫描线效果**
```kotlin
// 背景扫描线
Canvas(modifier = Modifier.fillMaxSize()) {
    for (i in 0..size.height.toInt() step 4) {
        drawLine(
            color = Color.White.copy(alpha = 0.03f),
            start = Offset(0f, i.toFloat()),
            end = Offset(size.width, i.toFloat())
        )
    }
}
```

**参考风格**:
- Bloomberg Terminal（专业金融风格）
- Cyberpunk 2077 游戏 UI
- Iron Man JARVIS 界面
- Minority Report 电影界面

---

### 任务 3：添加自动刷新

**在 HomeScreen.kt 中添加下拉刷新**:

```kotlin
@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun HomeScreen(viewModel: HomeViewModel = viewModel()) {
    val uiState by viewModel.uiState.collectAsState()
    val pullRefreshState = rememberPullToRefreshState()
    
    if (pullRefreshState.isRefreshing) {
        LaunchedEffect(true) {
            viewModel.refresh()
            pullRefreshState.endRefresh()
        }
    }
    
    Box(Modifier.nestedScroll(pullRefreshState.nestedScrollConnection)) {
        // 现有内容...
        
        PullToRefreshContainer(
            state = pullRefreshState,
            modifier = Modifier.align(Alignment.TopCenter)
        )
    }
}
```

---

## 📊 数据流说明

```
┌─────────────────────────────────────────────────────────────┐
│                      数据源层                                │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │
│  │ Finnhub │ │yfinance │ │  FRED   │ │  WSJ RSS Feed   │   │
│  │ 实时报价 │ │ 基本面  │ │宏观数据 │ │    新闻源       │   │
│  └────┬────┘ └────┬────┘ └────┬────┘ └───────┬─────────┘   │
│       └──────────┬┴──────────┬┘              │             │
│                  ▼           ▼               ▼             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                   Python 后端                         │ │
│  │  • 聚合数据                                           │ │
│  │  • 调用 Gemini AI 分析                                │ │
│  │  • 计算衍生指标（P/E vs 行业、52周位置、目标价空间）    │ │
│  └───────────────────────┬───────────────────────────────┘ │
│                          │                                 │
│           ┌──────────────┼──────────────┐                 │
│           ▼              ▼              ▼                 │
│    ┌────────────┐ ┌────────────┐ ┌────────────┐          │
│    │  飞书群    │ │ Android   │ │  未来:     │          │
│    │  (推送)    │ │   App     │ │  Web/iOS   │          │
│    └────────────┘ └────────────┘ └────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎓 金融术语速查

| 术语 | 含义 | 判断标准 |
|:----|:----|:--------|
| **P/E** | 股价÷每股收益 | <15便宜, 15-25合理, >40很贵 |
| **52周位置** | 当前价在一年高低点的位置 | 88%=接近高点强势, 36%=接近低点弱势 |
| **分析师评级** | 买入/持有/卖出人数 | 注意:"持有"往往意味着不看好 |
| **目标价空间** | 目标价vs当前价差距 | >+20%强烈看好, <0%看空 |
| **VIX** | 恐慌指数 | <15乐观, 20-30担忧, >40恐慌 |

---

## 🐛 已知问题

1. **Divider 组件警告**: Material 3 中 `Divider` 已被标记为 deprecated，未来版本需改为 `HorizontalDivider`，但当前版本还未完全支持

2. **模拟数据**: Android App 目前使用硬编码的模拟数据，需要连接后端才能显示真实数据

3. **图标缺失**: 为了快速修复编译错误，移除了 App 图标配置，需要重新添加

---

## 📝 给下一位开发者的提示词

```
你是一个 Android 开发专家，精通 Kotlin 和 Jetpack Compose。

我有一个名为 "My Personal Bloomberg" 的 Android 金融资讯 App 项目，需要你帮我继续开发。

项目当前状态：
- 已完成：基础 UI、市场概览、新闻卡片、术语解释弹窗、深色主题
- 使用模拟数据，没有连接真实 API
- 没有自动更新功能
- UI 比较简陋，缺乏科幻感

我需要你帮我完成以下任务：

【任务 1：后端 API】
- 使用 Python FastAPI 创建后端
- 集成 Finnhub API（股票数据）、yfinance（基本面）、Gemini AI（分析）
- 部署到 Vercel
- 提供接口：/api/market-overview, /api/news, /api/stock

【任务 2：Android 连接后端】
- 修改 ApiService.kt 连接真实 API
- 实现下拉刷新
- 添加加载状态和错误处理
- 启动时自动获取数据

【任务 3：UI 科幻美化】
- 添加渐变背景（深蓝色调）
- 添加霓虹发光边框效果
- 添加动画（数字滚动、卡片进入、脉冲效果）
- 添加背景扫描线
- 参考 Bloomberg Terminal + Cyberpunk 风格

项目技术栈：
- Android: Kotlin + Jetpack Compose + Material 3 + MVVM
- 后端: Python + FastAPI
- API: Finnhub, yfinance, Gemini, FRED

请先阅读项目代码结构，然后告诉我你的开发计划和时间估算。
```

---

## ✅ 你需要做的事情

1. **把 Android 项目上传到 GitHub**
   ```bash
   cd my-personal-bloomberg-android
   git init
   git add .
   git commit -m "Initial commit: MVP version"
   git remote add origin https://github.com/wang2-lat/my-personal-bloomberg.git
   git push -u origin main
   ```

2. **确保飞书机器人仓库的 Secrets 都配置正确**

3. **把这份文档（HANDOVER.md）放到项目根目录**

4. **把后端 API 模板（api_template.py）也放进去**

---

## 📞 项目信息

- **GitHub**: wang2-lat
- **飞书群**: JoyWang
- **开发时间**: 2026年1月
- **文档版本**: 1.0

---

*Made with ❤️ by Jeremy | Powered by Citadel AI Engine*
